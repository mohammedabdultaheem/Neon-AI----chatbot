{
  "name": "chatbot-root",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --prefix frontend && pip install -r backend/requirements.txt",
    "dev": "npx concurrently \"npm run dev --prefix frontend\" \"python backend/main.py\""
  }
}
